_schema-version: 3.3.0
ID: expense-management
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm install --legacy-peer-deps
        - npx cds build --production
modules:
  - name: expense-management-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: custom
      commands:
        - npm install --omit=dev --legacy-peer-deps
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: expense-management-auth
      - name: expense-management-db
      - name: expense-management-destination

  - name: expense-management-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: expense-management-db
  - name: expense-management-approuter
    type: approuter.nodejs
    path: app/router
    parameters:
      keep-existing-routes: true
      disk-quota: 256M
      memory: 256M
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true
      - name: expense-management-auth
      - name: expense-management-html5-runtime
    provides:
      - name: app-api
        properties:
          app-protocol: ${protocol}
          app-uri: ${default-uri}

  - name: expense-management-app-deployer
    type: com.sap.application.content
    path: gen
    build-parameters:
      build-result: app/
      requires:
        - name: expensemanagementexpenseportal
          artifacts:
            - expense-portal.zip
          target-path: app/
        - name: expensemanagementfinancedashboard
          artifacts:
            - finance-dashboard.zip
          target-path: app/
        - name: expensemanagementmanagerdashboard
          artifacts:
            - manager-dashboard.zip
          target-path: app/
    requires:
      - name: expense-management-auth
      - name: srv-api
      - name: expense-management-html5-repo-host
        parameters:
          content-target: true
          config:
            HTML5Runtime_enabled: true
    parameters:
      config:
        destinations:
          - Name: srv-api
            URL: ~{srv-api/srv-url}
            Authentication: NoAuthentication
            Type: HTTP
            ProxyType: Internet
            HTML5.ForwardAuthToken: true
            HTML5.DynamicDestination: true

  - name: expensemanagementexpenseportal
    type: html5
    path: app/expense-portal
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []
  - name: expensemanagementfinancedashboard
    type: html5
    path: app/finance-dashboard
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []
  - name: expensemanagementmanagerdashboard
    type: html5
    path: app/manager-dashboard
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []

  - name: expense-management-destinations
    type: com.sap.application.content
    requires:
      - name: expense-management-auth
        parameters:
          service-key:
            name: expense-management-auth-key
      - name: expense-management-html5-repo-host
        parameters:
          service-key:
            name: expense-management-html5-repo-host-key
      - name: srv-api
      - name: expense-management-destination
        parameters:
          content-target: true
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: expense-management-html5-repository
              ServiceInstanceName: expense-management-html5-repo-host
              ServiceKeyName: expense-management-html5-repo-host-key
              sap.cloud.service: expensemanagement.service
            - Name: expense-management-auth
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: expense-management-auth
              ServiceKeyName: expense-management-auth-key
              sap.cloud.service: expensemanagement.service

resources:
  - name: expense-management-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: expense-management-${org}-${space}
        tenant-mode: dedicated
        oauth2-configuration:
          credential-types:
            - "binding-secret"
            - "x509"
          redirect-uris:
            - https://*~{app-api/app-uri}/**
        role-collections:
          - name: 'Employee (expense-management ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.Employee'
          - name: 'Manager (expense-management ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.Manager'
          - name: 'Finance (expense-management ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.Finance'
          - name: 'Administrator (expense-management ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.Administrator'
    requires:
      - name: app-api
  - name: expense-management-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: expense-management-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
    requires:
      - name: srv-api

  - name: expense-management-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
  - name: expense-management-html5-runtime
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-runtime
